# 装修偏好共识 Agent：比赛演示包

这是一套约 3 分 20 秒的现场演示脚本。它只展示**可解释 MVP 的闭环**，不把合成预设说成真实训练或完整强化学习效果。

## 赛前 2 分钟检查

```powershell
cd 'D:\NUS-ISS AWS show me your agent\design-preference-agent'
python -m pip install -r requirements.txt
streamlit run app.py
```

浏览器打开后，侧栏的“比赛演示预设”包含所有案例。使用顺序是：

1. 选择“1. 对齐｜第 1 轮…”，点击“载入预设到输入区”；点击“重置当前项目”后，再点击主按钮“运行本轮偏好推断与方案评估”。
2. 选择“2. 对齐｜第 2 轮…”，载入后直接运行，**不要重置项目**。
3. 选择“3. 错位｜第 3 轮…”，载入后直接运行，**不要重置项目**。
4. 选择“4. 证据不足…”并运行；它使用新的项目 ID，因此会自动作为新屋主处理。
5. 选择“5. 异常密度…”并运行；同样是新的项目 ID。

若要从头再讲，重新载入第 1 轮并点击“重置当前项目”。

## 3 分 20 秒讲稿

### 0:00–0:25｜问题

> 装修沟通的难点不是屋主完全没有想法，而是想法分散在搜索词、图片、收藏和浏览行为里。设计师如果只看一句文本，很容易误解，产生反复修改。我们做的不是一个普通的装修推荐机器人，而是帮助屋主和设计师持续对齐偏好的 Agent。

### 0:25–0:50｜模型逻辑

> 每次浏览或沟通，我们把文本、图片标签、收藏或停留，以及证据质量打包成一个会话证据。系统结合上一时刻的偏好状态和当前证据，用贝叶斯方式更新动态偏好。注意力、密度和异常密度只影响证据权重，因此系统既能给出偏好，也能解释自己有多确定。

### 0:50–1:20｜案例一：首次确认，但不仓促推荐

执行预设 1。

> 这里屋主已经明确提到新中式、原木、暖光、收纳和预算；候选方案也符合预算、面积和结构约束。注意系统仍建议展示对比 Mood Board，而不是立刻强推方案。原因是：一次高质量会话可以产生方向性，但它还不足以让模型过度自信。

指向“动态偏好后验”“证据质量与注意力”“方案执行度”和“下一步动作”四块结果。预期：匹配方案执行度约 0.73、分类为一致，但动作是 `SHOW`。

### 1:20–1:45｜案例二：连续证据后展示方案

执行预设 2；保持同一项目 ID，不重置。

> 第二次高质量确认进入同一屋主的时间序列。上一时刻的偏好不会被丢掉，而是和新证据一起更新。此时不确定性下降，匹配方案可以被展示。我们从“先澄清”变成“可以展示”，这就是动态偏好而不是一次性分类。

预期：匹配方案为 `aligned`，下一步动作是 `PRESENT`。

### 1:45–2:10｜案例三：偏好明确，但方案错位

执行预设 3；仍保持同一项目 ID，不重置。

> 这里屋主偏好没有改变；我只把设计师候选方案换成一个深色轻奢方案。系统会保留同一偏好后验，逐维计算方案错位，并给出“先修改候选方案”的建议。它说明偏好推断和方案评估是两条独立链路：用户理解正确，不代表当前设计方案正确。

预期：`misaligned`，下一步动作 `RECOMMEND`。

### 2:10–2:32｜案例四：证据不足不等于错位

执行预设 4。

> 现在屋主只说“想装修得舒服一点”。一个普通分类器可能会强行贴标签；我们输出的是“待屋主确认”，并追问关键约束。证据不足不等于设计师做错，也不应该被误判为偏好错位。

预期：`needs_owner_confirmation`，下一步动作 `ASK`。

### 2:32–2:52｜案例五：异常密度不让模型盲信

执行预设 5。

> 这里模拟短时间重复浏览原木内容。系统检测到高重复和异常密度，于是降低本次证据质量、增大观测噪声，并暂缓结论。异常密度不直接改变真实偏好，只影响这批证据值不值得相信。

预期：异常密度接近 100%、证据质量约 25%、下一步动作 `DEFER`。

### 2:52–3:20｜强化学习边界、数据与收束

> 当前 Demo 的下一步动作采用可解释、安全优先的规则策略；强化学习在未来负责从 Ask、Show、Confirm、Recommend、Present、Defer 中选择长期收益更高的动作，而不直接改写屋主偏好。真实部署会收集匿名化的“系统动作—屋主反馈—方案接受或返工”日志，并按项目和时间严格划分训练、验证、测试。我们的核心价值是：把难以言说、会变化的装修偏好，变成可解释、可量化、可行动的设计沟通依据。

## 评委追问速答

| 问题 | 建议回答 |
|---|---|
| 为什么不用普通分类模型？ | 分类只输出一个标签；我们的任务需要同时保留多偏好、时间变化和不确定性，避免把一次点击当作真实偏好。 |
| 异常密度为什么不直接降低偏好？ | 它描述的是证据可靠性，不是屋主真实偏好的反向信号；因此它进入质量分与观测噪声，而不是直接改写偏好状态。 |
| 强化学习在哪里？ | RL 只选下一步沟通动作；目前没有真实干预日志，所以 Demo 使用安全、可解释的策略基线，不声称已训练出最优 RL。 |
| 如何避免过拟合或泄漏？ | 以项目/屋主和全局时间隔离训练、验证、测试；只在训练集扩充，测试集冻结。详见 `DATA_PLAN.md`。 |

## 不要说的话

- 不要说“我们已用真实屋主数据训练出准确模型”。当前预设是合成演示会话。
- 不要说“我们已经证明 RL 提升了满意度”。当前只实现了 RL 的安全决策接口和规则型基线。
- 不要把普通点击、停留或高频浏览直接说成真实满意度或真实偏好。
